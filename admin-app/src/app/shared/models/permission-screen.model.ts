export class PermissionScreen {
    id: string;

    name: string;

    parentId: string;

    hasCreate: boolean;

    hasUpdate: boolean;

    hasDelete: boolean;

    hasView: boolean;

    hasApprove: boolean;
}